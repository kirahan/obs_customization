#pragma once

#include <windows.h>
#include <initguid.h>

#ifdef VIRTUALCAM_ENABLED
DEFINE_GUID(CLSID_OBS_VirtualVideo,
            0x,
            0x,
            0x,
            0x,
            0x,
            0x,
            0x,
            0x,
            0x,
            0x,
            0x);
#endif
